# Отчёт о развертывании и работе сервиса

## 1\. Введение

Данный документ содержит подробное описание процесса развертывания, тестирования и эксплуатации проекта  - полноценного микросервисного приложения, реализующего семантический глоссарий с визуализацией графа терминов. Проект включает в себя:

- **Backend** (Python + gRPC) -- предоставляет API для управления терминами (CRUD) и получения структуры графа.
- **Frontend** (HTML, CSS, JavaScript, vis.js) -- отображает список терминов и интерактивный граф, взаимодействует с бэкендом через gRPC-web.
- **Envoy proxy** -- обеспечивает трансляцию gRPC-web в gRPC.
- **Docker** -- контейнеризация всех компонентов, оркестрация через Docker Compose.

Цель развертывания - проверить корректность реализации всех заявленных функций: просмотр терминов, визуализация семантических связей, добавление, редактирование и удаление терминов с автоматическим обновлением графа и связей.

---

## 2\. Предварительные требования

Перед началом убедитесь, что на вашей системе установлены:

- **Docker** версии 24.0.0 или выше (проверить: `docker --version`).
- **Docker Compose** версии 2.20.0 или выше (`docker-compose --version`).
- **Операционная система**: Linux, macOS или Windows с WSL2 (рекомендуется).
- **Утилита `grpcurl`** (опционально) -- для тестирования gRPC API вручную.
- **Git** -- для клонирования репозитория (если ещё не склонирован).

---

## 3\. Подготовка к развёртыванию

### 3.1. Клонирование репозитория

```bash
git clone https://github.com/darksidefriend/python_task4
cd glossary-mindmap

```

### 3.2. Проверка наличия сгенерированных protobuf-файлов

Фронтенд использует сгенерированные JavaScript-файлы из proto-спецификации. Они должны находиться в папке `frontend/static`. Обычно они уже включены в репозиторий, но если их нет, необходимо сгенерировать их вручную.

Выполните следующую команду (одна строка) из корня проекта:

bash

docker run --rm -v $(pwd):/workspace -w /workspace node:18-slim /bin/bash -c "apt-get update && apt-get install -y unzip curl && curl -L https://github.com/protocolbuffers/protobuf/releases/download/v21.12/protoc-21.12-linux-x86_64.zip -o protoc.zip && unzip protoc.zip -d /usr/local && rm protoc.zip && curl -L https://github.com/grpc/grpc-web/releases/download/1.5.0/protoc-gen-grpc-web-1.5.0-linux-x86_64 -o /usr/local/bin/protoc-gen-grpc-web && chmod +x /usr/local/bin/protoc-gen-grpc-web && npm install -g protoc-gen-js && mkdir -p frontend/static && protoc -I proto proto/glossary.proto --js_out=import_style=commonjs,binary:frontend/static --grpc-web_out=import_style=commonjs,mode=grpcwebtext:frontend/static"

После выполнения в папке `frontend/static` появятся файлы:

-   `glossary_pb.js`

-   `glossary_grpc_web_pb.js`

* * * * *

4\. Процесс развертывания
-------------------------

### 4.1. Запуск контейнеров

Из корня проекта выполните команду:

bash

docker-compose up --build

Пояснение:

-   `--build` заставляет Docker пересобрать образы, даже если они уже существуют (полезно при первом запуске или после изменений).

-   Команда запускает три сервиса: `backend`, `envoy`, `frontend`.

### 4.2. Логи запуска

При успешном старте вы увидите в консоли следующие сообщения (последовательность может отличаться):

text

backend-1   | INFO:__main__:Loaded 15 terms
backend-1   | INFO:__main__:Server started on port 50051
envoy-1     | [2026-02-25 10:00:00.123][1][info][main] [source/server/server.cc:...] starting server...
frontend-1  | /docker-entrypoint.sh: /docker-entrypoint.d/ is not empty, will attempt to perform configuration
...
frontend-1  | 2026/02/25 10:00:01 [notice] 1#1: start worker processes

Это означает, что все сервисы готовы к работе.

* * * * *

5\. Проверка работоспособности
------------------------------

### 5.1. Доступ к веб-интерфейсу

Откройте браузер и перейдите по адресу: [http://localhost:8080](http://localhost:8080/)

Ожидаемый результат:

-   Заголовок страницы: "Machine Learning Glossary with Semantic Graph".

-   Слева -- список терминов (не менее 15).

-   Справа -- пустая область, которая через секунду заполнится интерактивным графом.

### 5.2. Тестирование функций

#### 5.2.1. Просмотр деталей термина через список

-   Кликните на любой термин в левом списке (например, "Deep Learning").

-   Ожидаемое поведение: Справа, в панели деталей, отобразятся:

    -   Название термина.

    -   Определение.

    -   Список источников (ссылки).

    -   Список связей (например, "uses → Neural Network").

-   Фактически: Все данные присутствуют, ссылки кликабельны и открываются в новой вкладке.

#### 5.2.2. Просмотр деталей термина через граф

-   Кликните на любой узел в графе.

-   Ожидаемое поведение: Откроется модальное окно с полной информацией о термине, а также кнопками Edit и Delete.

-   Фактически: Модальное окно появляется, информация соответствует выбранному узлу.

#### 5.2.3. Добавление нового термина

1.  Нажмите кнопку "Add New Term" над списком.

2.  В открывшемся модальном окне заполните поля:

    -   Term Name: `CNN`

    -   Definition: `Convolutional Neural Network -- a class of deep neural networks.`

    -   Sources (URLs): добавьте ссылку:

        -   URL: `https://en.wikipedia.org/wiki/Convolutional_neural_network`

        -   Title: `Wikipedia: CNN`

    -   Relations: добавьте связь:

        -   Target term: `Neural Network`

        -   Relation type: `is-a`

3.  Нажмите Save.

4.  Ожидаемое поведение: Модальное окно закрывается, список терминов обновляется (появляется "CNN"), граф перерисовывается с новым узлом и ребром.

5.  Фактически: Все изменения отображаются корректно.

#### 5.2.4. Редактирование термина

1.  Кликните на узел "CNN" в графе (или на соответствующий пункт в списке).

2.  В модальном окне нажмите ✏️ Edit.

3.  В форме измените определение, добавьте ещё одну ссылку или связь.

4.  Нажмите Save.

5.  Ожидаемое поведение: Модальное окно закрывается, детали термина в панели справа обновляются, граф отражает изменения (например, новые рёбра).

6.  Фактически: Изменения применяются без ошибок.

#### 5.2.5. Удаление термина

1.  Кликните на узел "CNN" (или выберите его из списка).

2.  В модальном окне нажмите 🗑️ Delete.

3.  Подтвердите удаление в диалоговом окне браузера.

4.  Ожидаемое поведение: Термин исчезает из списка и с графа. Все связи, которые указывали на него или от него, также удаляются (например, ребро "is-a" между "CNN" и "Neural Network" пропадает).

5.  Фактически: Интерфейс обновляется, граф становится чистым от удалённого узла.

### 5.3. Тестирование gRPC API с помощью `grpcurl`

Убедитесь, что бэкенд доступен на порту `50051` (он проброшен на хост). Выполните несколько запросов.

#### 5.3.1. Получить все термины

bash

grpcurl -plaintext -d '{}' localhost:50051 glossary.GlossaryService/GetAllTerms

Ожидаемый результат: JSON-список всех терминов с определениями и связями. Ответ не должен содержать ошибок.

#### 5.3.2. Получить конкретный термин

bash

grpcurl -plaintext -d '{"name": "Deep Learning"}' localhost:50051 glossary.GlossaryService/GetTermByName

Ожидаемый результат: Объект термина "Deep Learning".

#### 5.3.3. Добавить термин через API

bash

grpcurl -plaintext -d '{
  "name": "API Test",
  "definition": {
    "text": "Term added via gRPC",
    "links": [{"url": "http://example.com", "title": "Example"}]
  }
}' localhost:50051 glossary.GlossaryService/AddTerm

Ожидаемый результат: `{"success": true, "message": "Term 'API Test' added successfully"}`

#### 5.3.4. Удалить термин через API

bash

grpcurl -plaintext -d '{"name": "API Test"}' localhost:50051 glossary.GlossaryService/DeleteTerm

Ожидаемый результат: `{"success": true, "message": "Term 'API Test' deleted successfully"}`

#### 5.3.5. Получить граф

bash

grpcurl -plaintext -d '{}' localhost:50051 glossary.GlossaryService/GetGraph

Ожидаемый результат: Список узлов и рёбер.

Все запросы должны выполняться без ошибок и возвращать корректные данные.


6\. Заключение
--------------

В ходе развертывания проекта были выполнены все запланированные шаги:

-   Успешно подняты три контейнера (backend, envoy, frontend).

-   Проверена работоспособность веб-интерфейса: отображение списка терминов, интерактивный граф, модальные окна с деталями.

-   Протестированы CRUD-операции: добавление, редактирование и удаление терминов -- все изменения корректно отражаются в интерфейсе и графе.

-   Выполнены ручные запросы к gRPC API через `grpcurl`, подтверждена корректность ответов.

Все компоненты взаимодействуют между собой согласно архитектуре: фронтенд через gRPC-web обращается к Envoy, который проксирует запросы к бэкенду по gRPC. Бэкенд хранит данные в памяти, инициализируясь из `data.json`.